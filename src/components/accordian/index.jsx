//single selection
//multiple selection
import {useSelector} from 'react-redux';
export default function Accordian(){

    const [selected, setSelected] = useSelector(null);

    return <div>
        Accordian
    </div>
};